import { createTheme } from '@mui/material'

export const appTheme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#ed5b2f',
    },
    secondary: {
      main: '#f50057',
    },
    background: {
      default: '#f2f2f2',
    },
  },
})
